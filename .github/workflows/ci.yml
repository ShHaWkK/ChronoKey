name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
      - name: Build
        run: cargo build --workspace --all-targets
      - name: Test
        run: cargo test --workspace --all-targets
      - name: Upload linux binaries
        uses: actions/upload-artifact@v3
        with:
          name: chronokey-linux
          path: |
            target/debug/chronokey
            target/debug/chronokey-issuer

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
      - name: Build
        run: cargo build --workspace --all-targets
      - name: Test
        run: cargo test --workspace --all-targets
      - name: Upload windows binaries
        uses: actions/upload-artifact@v3
        with:
          name: chronokey-windows
          path: |
            target\debug\chronokey.exe
            target\debug\chronokey-issuer.exe
